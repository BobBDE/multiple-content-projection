import { AdvancedContainerHeaderDirective } from './advanced-container-header.directive';

describe('AdvancedContainerHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvancedContainerHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
