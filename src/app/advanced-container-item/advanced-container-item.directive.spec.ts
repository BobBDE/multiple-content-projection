import { AdvancedContainerItemDirective } from './advanced-container-item.directive';

describe('AdvancedContainerItemDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvancedContainerItemDirective();
    expect(directive).toBeTruthy();
  });
});
